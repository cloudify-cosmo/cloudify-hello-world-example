machine:
  python:
    - version: 2.7.9

dependencies:
  override:
    - pip install flake8
    - pip install -r test-requirements.txt
    - pip install .


test:
  override:
    - flake8 .
    - nosetests suites/tests
